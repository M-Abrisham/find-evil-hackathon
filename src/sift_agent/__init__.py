"""SIFT forensic agent — application package.

The forensic LLM/tool ledger telemetry lives in :mod:`sift_agent.telemetry`,
a thin wrapper over the Na0S ``judge`` primitives (cost / audit / rate-limit).
See ``docs/contribution-table.md`` for reuse provenance.
"""
